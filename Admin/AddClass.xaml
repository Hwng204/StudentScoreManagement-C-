<Window x:Class="StudentScoreManager.Admin.AddClass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentScoreManager.Admin"
        mc:Ignorable="d"
        Title="Add Class" Height="570" Width="820" WindowStartupLocation="CenterScreen">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2.5*" />
            <ColumnDefinition Width="3.5*" />
        </Grid.ColumnDefinitions>

        <!-- LEFT: Class Info -->
        <GroupBox Header="Class Information" Grid.Column="0" Margin="10" FontWeight="Bold" FontSize="14">
            <StackPanel Margin="10">
                <TextBlock Text="Class Code:" FontSize="13" />
                <TextBox x:Name="txtClassCode" Margin="0,5" FontSize="13"/>

                <TextBlock Text="Class Name:" FontSize="13"/>
                <TextBox x:Name="txtClassName" Margin="0,5" FontSize="13"/>

                <TextBlock Text="Slot:" FontSize="13"/>
                <TextBox x:Name="txtSlot" Margin="0,5" FontSize="13"/>
            </StackPanel>
        </GroupBox>

        <!-- RIGHT: Subject + Teacher -->
        <GroupBox Header="Subjects and Teachers" Grid.Column="1" Margin="10" FontWeight="Bold" FontSize="14">
            <StackPanel Margin="10">
                <!-- Subject Section -->
                <TextBlock Text="Search Subject:" FontSize="13" FontWeight="SemiBold"/>
                <TextBox x:Name="txtSearchSubject" Margin="0,5" FontSize="13" TextChanged="txtSearchSubject_TextChanged"/>

                <TextBlock Text="Select Subject:" Margin="0,10,0,5" FontWeight="SemiBold"/>
                <ListBox x:Name="lstSubjects"
                         DisplayMemberPath="SubjectName"
                         SelectedValuePath="SubjectId"
                         SelectionMode="Single"
                         Height="100"
                         FontSize="13"/>

                <!-- Teacher Section -->
                <TextBlock Text="Search Teacher:" Margin="0,15,0,0" FontSize="13" FontWeight="SemiBold"/>
                <TextBox x:Name="txtSearchTeacher" Margin="0,5" FontSize="13" TextChanged="txtSearchTeacher_TextChanged"/>

                <TextBlock Text="Select Teacher:" Margin="0,10,0,5" FontWeight="SemiBold"/>
                <ListBox x:Name="lstTeachers"
                         DisplayMemberPath="FullName"
                         SelectedValuePath="TeacherId"
                         SelectionMode="Single"
                         Height="100"
                         FontSize="13"/>
            </StackPanel>
        </GroupBox>

        <!-- Buttons -->
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" HorizontalAlignment="Center" Margin="0,440,0,0">
            <Button x:Name="btnTurnOut" Content="← Back" Width="120" Height="35" Margin="10" FontSize="13" Click="btnTurnOut_Click"/>
            <Button x:Name="btnSave" Content="➕ Add" Width="120" Height="35" Margin="10" FontSize="13" Click="btnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>

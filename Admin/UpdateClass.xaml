<Window x:Class="StudentScoreManager.Admin.UpdateClass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentScoreManager.Admin"
        mc:Ignorable="d"
        Title="UpdateClass" Height="520" Width="800">
    <Grid Margin="10">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*" />
            <ColumnDefinition Width="3*" />
        </Grid.ColumnDefinitions>

        <!-- LEFT: Class Info -->
        <StackPanel Grid.Column="0" Margin="10" VerticalAlignment="Top">
            <TextBlock Text="Class Code:" />
            <TextBox x:Name="txtClassCode" Margin="0,5" IsReadOnly="True"/>

            <TextBlock Text="Class Name:" />
            <TextBox x:Name="txtClassName" Margin="0,5" />

            <TextBlock Text="Slot:" />
            <TextBox x:Name="txtSlot" Margin="0,5" />
        </StackPanel>

        <!-- RIGHT: Lists -->
        <StackPanel Grid.Column="1" Margin="10" VerticalAlignment="Top">
            <TextBlock Text="Search Subject:" FontWeight="Bold" />
            <TextBox x:Name="txtSearchSubject" Margin="0,5" TextChanged="txtSearchSubject_TextChanged"/>

            <TextBlock Text="Select Subject:" FontWeight="Bold" Margin="0,5"/>
            <ListBox x:Name="lstSubjects" Height="120"
                     DisplayMemberPath="SubjectName"
                     SelectedValuePath="SubjectId"
                     SelectionMode="Single"/>

            <TextBlock Text="Search Teacher:" FontWeight="Bold" Margin="0,15,0,0"/>
            <TextBox x:Name="txtSearchTeacher" Margin="0,5" TextChanged="txtSearchTeacher_TextChanged"/>

            <TextBlock Text="Select Teacher:" FontWeight="Bold" Margin="0,5"/>
            <ListBox x:Name="lstTeachers" Height="120"
                     DisplayMemberPath="FullName"
                     SelectedValuePath="TeacherId"
                     SelectionMode="Single"/>
        </StackPanel>

        <!-- Buttons -->
        <StackPanel Grid.ColumnSpan="2" Orientation="Horizontal" 
                    HorizontalAlignment="Center" Margin="0,410,0,0">
            <Button Content="Hủy" Width="100" Margin="10" Click="btnCancel_Click"/>
            <Button Content="Lưu" Width="100" Margin="10" Click="btnSave_Click"/>
        </StackPanel>
    </Grid>
</Window>

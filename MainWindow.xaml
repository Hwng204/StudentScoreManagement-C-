<Window x:Class="StudentScoreManager.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentScoreManager"
        mc:Ignorable="d"
        Title="Đăng nhập" Height="400" Width="450" Background="#f0f2f5"
        WindowStartupLocation="CenterScreen">
    <Grid>
        <Border Background="White" CornerRadius="10" Padding="20" Margin="40">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="Auto"/>
                </Grid.RowDefinitions>

                <!-- Tiêu đề -->
                <TextBlock Text="Đăng nhập" FontSize="20" FontWeight="Bold"
                           HorizontalAlignment="Center" Margin="0,0,0,20"/>

                <!-- Email -->
                <StackPanel Grid.Row="1" Margin="0,0,0,10">
                    <TextBlock Text="Email:" FontSize="14" Margin="0,0,0,5"/>
                    <TextBox x:Name="emailLogin" Height="30" FontSize="14" Padding="5"
                             Width="250" HorizontalAlignment="Center"/>
                </StackPanel>

                <!-- Mật khẩu -->
                <StackPanel Grid.Row="2" Margin="0,0,0,20">
                    <TextBlock Text="Mật khẩu:" FontSize="14" Margin="0,0,0,5"/>
                    <PasswordBox x:Name="passwordLogin" Height="30" FontSize="14" Padding="5"
                             Width="250"/>
                </StackPanel>

                <!-- Nút đăng nhập -->
                <Button Grid.Row="3" Content="Đăng nhập" Width="150" Height="35"
                        Background="#0078D7" Foreground="White" FontWeight="SemiBold"
                        HorizontalAlignment="Center" Cursor="Hand"
                        BorderThickness="0" 
                        Click="Button_Click"/>
            </Grid>
        </Border>
    </Grid>
</Window>

<Window x:Class="StudentScoreManager.Teacher.StudenstInClass"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:StudentScoreManager.Teacher"
        mc:Ignorable="d"
        Title="StudenstInClass" Height="450" Width="800">
    <Grid Margin="10">
        <StackPanel>
            <!-- Thông tin lớp -->
            <StackPanel Orientation="Horizontal" Margin="0,0,0,10">
                <TextBlock Text="Class Code:" FontWeight="Bold"/>
                <TextBlock x:Name="classCodeText" Margin="5,0"/>
                <TextBlock Text=" | Subject:" FontWeight="Bold" Margin="20,0"/>
                <TextBlock x:Name="subjectNameText" Margin="5,0"/>
            </StackPanel>

            <!-- DataGrid điểm -->
            <DataGrid x:Name="scoreDataGrid" AutoGenerateColumns="False" CanUserAddRows="False" Margin="0,10" 
                      CellEditEnding="scoreDataGrid_CellEditEnding" HeadersVisibility="Column">
                <!-- Cột cố định -->
                <DataGrid.Columns>
                    <DataGridTextColumn Header="Student Code" Binding="{Binding StudentCode}" IsReadOnly="True" Width="0.1*"/>
                    <DataGridTextColumn Header="Full Name" Binding="{Binding FullName}" IsReadOnly="True" Width="0.5*"/>
                </DataGrid.Columns>
            </DataGrid>

            <StackPanel Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,10,0,0">
                <Button Content="Save" Width="100" Click="Save_Click" Margin="0,0,10,0"/>
                <Button Content="Back" Width="100" Click="Back_Click"/>
            </StackPanel>
        </StackPanel>
    </Grid>
</Window>
